---
description: Generate implementation tasks for a specification
argument-hint: <feature-name> [-y] [--sequential]
---

Invoke the `yy-dev:spec-tasks` skill to generate tasks for feature $ARGUMENTS.
