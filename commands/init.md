---
description: 初始化项目 — 创建 steering 项目记忆和目录结构
argument-hint: [project-description]
allowed-tools: Bash, Read, Write, Edit, Glob, Grep, LS, AskUserQuestion, TaskCreate, TaskUpdate, TaskList
---

Invoke the `yy:init` skill and follow it exactly as presented to you.

Pass $ARGUMENTS as the project description. If no arguments, run in brownfield (auto-detect) mode.

IMPORTANT: You MUST ask clarifying questions BEFORE generating any files. Do NOT skip the interactive Q&A phase.
