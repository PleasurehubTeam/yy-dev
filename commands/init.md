---
description: 初始化项目 — 创建 steering 项目记忆和目录结构
argument-hint: [project-description]
---

Invoke the `yy:init` skill to initialize this project.

Pass $ARGUMENTS as the project description. If no arguments, run in brownfield (auto-detect) mode.
